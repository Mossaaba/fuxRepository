apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-web-app
  namespace: default  # Ensure the namespace is correct
spec:
  replicas: 3
  selector:
    matchLabels:
      app: python-web-app
  template:
    metadata:
      labels:
        app: python-web-app
    spec:
      containers:
      - name: python-web-app
        image: mouss1959/app:5
        ports:
        - containerPort: 5001
